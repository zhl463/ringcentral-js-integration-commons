export const defaultAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const callLogAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const multiDialingPlanIncludingUSCAAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const onlyOneUSCADialingPlanAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const onlyOneNoneUSCADialingPlanAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const multiNoneUSCADialingPlanAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};

export const smsAccount = {
  username: 'Replace it with related phoneNumber(required)',
  password: 'Replace it with related password(required)',
  existingExtension: 'Replace it with existing extension(optional)',
  serviceNumber: 'Replace it with your service number(optional)'
};
